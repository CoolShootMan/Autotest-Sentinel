# Per-case parameters for test_linda_t2302_verify_user_can_login_with_email_and_password
# Inherits env/Release.yaml (case-first merge). Shared `password` lives in global env.
variables:
  account_email_curator: linda.zhou.ext+00@1m.app
