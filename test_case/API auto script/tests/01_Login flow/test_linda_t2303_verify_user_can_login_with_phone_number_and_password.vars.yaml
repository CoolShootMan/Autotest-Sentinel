# Per-case parameters for test_linda_t2303_verify_user_can_login_with_phone_number_and_password
# Inherits env/Release.yaml (case-first merge). Shared `password` lives in global env.
variables:
  phoneNumber: "+16502396646"
